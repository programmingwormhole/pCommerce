class SharedServices {
  static Future<void> storeDataOrToken () async {
    // Store the token or any others data if need
  }

  static Future<void> logout () async {
    // remove all the stored data and navigate to login screen

    // Get.offAllNamed(RouteNames.login);
  }
}